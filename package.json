{
  "name": "agencykit-11ty-starter",
  "version": "1.1.0",
  "description": "",
  "main": ".eleventy.js",
  "type": "module",
  "dependencies": {
    "@agency-kit/notion-cms": "^0.10.0",
    "@changesets/cli": "^2.27.1",
    "dotenv": "^16.4.1",
    "rimraf": "^5.0.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.0.0-alpha.4"
  },
  "scripts": {
    "serve": "rimraf _site && npx @11ty/eleventy --serve",
    "watch": "rimraf _site && npx @11ty/eleventy --watch",
    "build": "rimraf _site && PROD=true npx @11ty/eleventy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "changeset version"
  },
  "author": "",
  "license": "ISC"
}
